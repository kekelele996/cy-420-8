import { onMounted, onUnmounted } from 'vue'; import { useGameStore } from '../stores/gameStore'; export function useKeyboard(){ const store=useGameStore(); const handler=(e:KeyboardEvent)=>{ if(e.key==='ArrowUp')store.move(0,-1); if(e.key==='ArrowDown')store.move(0,1); if(e.key==='ArrowLeft')store.move(-1,0); if(e.key==='ArrowRight')store.move(1,0); }; onMounted(()=>window.addEventListener('keydown',handler)); onUnmounted(()=>window.removeEventListener('keydown',handler)); }
