import { CellType } from '../constants/cell'; import type { Cell } from '../models/cell'; import { logGame } from './gameLogger'; export function fillTerritory(map:Cell[][], owner_id:string){ const captured:Cell[]=[]; for(const row of map){ for(const cell of row){ if(cell.type===CellType.EMPTY && cell.x>2 && cell.y>2 && cell.x<8 && cell.y<8){ cell.type=CellType.TERRITORY; cell.owner_id=owner_id; captured.push(cell); } } } logGame('FLOOD_FILL',{count:captured.length}); return captured; }
