import { LOG_TEMPLATES } from '../constants/logTemplates'; const logs:{at:string;message:string}[]=[]; export function logGame(key:keyof typeof LOG_TEMPLATES, vars:Record<string,string|number>={}){ let message=LOG_TEMPLATES[key]; for(const [k,v] of Object.entries(vars)) message=message.replace('{'+k+'}',String(v)); logs.unshift({at:new Date().toISOString(),message}); logs.splice(80); return message;} export const readLogs=()=>logs;
