import { PlayerStatus } from '../constants/player'; import { RoomStatus } from '../constants/room'; import { CellType } from '../constants/cell'; export const playerStatusText:Record<PlayerStatus,string>={alive:'存活',dead:'已淘汰',observer:'观战',disconnected:'断线'}; export const roomStatusText:Record<RoomStatus,string>={waiting:'等待中',playing:'进行中',ended:'已结束'}; export const cellTypeText:Record<CellType,string>={empty:'空地',trail:'路径',territory:'领土'}; export const scoreText=(v:number)=>v.toLocaleString('zh-CN'); export const colorName=(color:string)=>({'#2f80ed':'蓝','#eb5757':'红','#27ae60':'绿','#f2c94c':'黄'}[color]||color);
