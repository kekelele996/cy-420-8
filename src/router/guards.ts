import type { Router } from 'vue-router'; import { PlayerStatus } from '../constants/player'; import { RoomStatus } from '../constants/room'; import { CellType } from '../constants/cell'; export function installGuards(router:Router){ router.beforeEach((to)=>{ to.meta.playerStatus=PlayerStatus.ALIVE; to.meta.roomStatus=RoomStatus.WAITING; to.meta.cellType=CellType.EMPTY; return true; }); }
