<template><article class="panel"><h3>{{ room.name }}</h3><p>{{ roomStatusText[room.status] }} · {{ room.players.length }}/{{ room.max_players }}</p><GameButton @click="$emit('join', room.id)">加入房间</GameButton></article></template><script setup lang="ts">import type { Room } from '../../models/room'; import { roomStatusText } from '../../utils/formatters'; import GameButton from './GameButton.vue'; defineProps<{room:Room}>(); defineEmits<{join:[id:string]}>();</script>
