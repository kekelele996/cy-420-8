export const LOG_TEMPLATES={ CONNECT:'Socket connect {id}', RECONNECT:'Socket reconnect {count}', ROOM_CREATE:'Room[name={name}] create', ROOM_JOIN:'Room[id={id}] join', ROOM_LIST:'Room list refresh', ROOM_READY:'Room[id={id}] ready', GAME_START:'Game[room={id}] start', GAME_TICK:'Game tick {tick}', PLAYER_MOVE:'Player[id={id}] move {x},{y}', PLAYER_STATUS:'Player[id={id}] status {status}', PLAYER_KILL:'Player[id={id}] kill {target}', TERRITORY_CAPTURE:'Player[id={id}] capture {count}', CELL_UPDATE:'Cell[{x},{y}] type {type}', FLOOD_FILL:'Flood fill {count}', MAP_RENDER:'Map render {tick}', LEADERBOARD_RENDER:'Leaderboard render', AVATAR_RENDER:'Avatar status {status}', MESSAGE_PUSH:'Message {text}', ERROR_BOUNDARY:'Error {message}', STORAGE_WRITE:'Storage {key} write', STORAGE_READ:'Storage {key} read', WS_SEND:'WS send {type}', WS_RECEIVE:'WS receive {type}', OBSERVER_SWITCH:'Observer switch', ROOM_END:'Room[id={id}] end', COLOR_PICK:'Color pick {color}', QUICK_JOIN:'Quick join', RULE_OPEN:'Rule open', BOT_SPAWN:'Bot spawn {id}', BOT_MOVE:'Bot move {id}', SCORE_SAVE:'Score save {score}', DISCONNECT:'Socket disconnect', SAFE_RETRY:'Safe retry' };
